GEMINI_API_KEY=

# Optional: HTTPS URL to a JSON catalog matching assets/on_device_models.json
# When set, Folio refreshes the on-device model list (24h cache + Settings refresh).
# ON_DEVICE_MODELS_CATALOG_URL=https://example.com/folio_on_device_models.json
