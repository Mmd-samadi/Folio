import 'package:flutter/material.dart';

/// Folio design tokens from Figma (dark theme only).
abstract final class FolioColors {
  static const background = Color(0xFF0A0A0A);
  static const surface = Color(0xFF141414);
  static const surfaceElevated = Color(0xFF1E1E1E);
  static const border = Color(0xFF2A2A2A);
  static const accent = Color(0xFF6C63FF);
  static const onAccent = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF888888);
  static const textDim = Color(0xFF444444);
  static const warningBg = Color(0xFF3A2A0A);
  static const warningBorder = Color(0xFFE6A23C);
  static const warningText = Color(0xFFFFD07B);
  static const danger = Color(0xFFE55C5C);
  static const offlineBg = Color(0xFF3A1A1A);
  static const offlineText = Color(0xFFFFB4B4);
  static const pdfPage = Color(0xFFF2F2F2);
  static const pdfText = Color(0xFF333333);

  static const radiusCard = 8.0;
  static const radiusButton = 4.0;
  static const radiusSheet = 12.0;
  static const radiusFab = 28.0;
}
